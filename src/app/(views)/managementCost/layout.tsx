import PageLayout from '@/components/common/PageLayout'

export default function ManagementCostLayout({ children }: { children: React.ReactNode }) {
  return <PageLayout entity="관리비">{children}</PageLayout>
}
