import PageLayout from '@/components/common/PageLayout'

export default function OutsourcingContractLayout({ children }: { children: React.ReactNode }) {
  return <PageLayout entity="권한 그룹">{children}</PageLayout>
}
