import PageLayout from '@/components/common/PageLayout'

export default function OutsourcingCompanyLayout({ children }: { children: React.ReactNode }) {
  return <PageLayout entity="외주업체관리">{children}</PageLayout>
}
